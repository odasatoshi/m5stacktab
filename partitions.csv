# Name,   Type, SubType, Offset,   Size, Flags
# app は日本語フォント (efont CJK) と esp-hosted を含むので 1.5MB では入らない。
# dict は SKK 辞書 (UTF-8 化した SKK-JISYO.L で約 5.9MB) を置くため (#8)。
nvs,      data, nvs,     0x9000,   0x6000,
phy_init, data, phy,     0xf000,   0x1000,
factory,  app,  factory, 0x10000,  6M,
dict,     data, spiffs,  ,         8M,
# SSH の秘密鍵。NVS ではなく専用領域にするのは、長さ制限と base64 経由を避けるため。
# 書き込み: python $IDF_PATH/components/partition_table/parttool.py --port PORT \
#             write_partition --partition-name sshkey --input ~/.ssh/id_ecdsa
sshkey,   data, 0x40,    ,         64K,
